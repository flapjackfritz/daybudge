import React from 'react';
import { Button } from 'semantic-ui-react';

class App extends React.Component {
  render() {
    return (
      <div>
        {/*
          Display current amount spent today
          Display amount spent yesterday
          Display carry-over
        */}
        <Button>Semantic UI Loaded!</Button>
      </div>
    );
  }
}

export default App;
